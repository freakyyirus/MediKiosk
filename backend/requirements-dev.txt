# Backend test/development dependencies (used by `pytest` and CI).
# Production deps come from requirements.txt via pyproject.toml.
#
#   pip install -r requirements-dev.txt
#
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
factory-boy>=3.3.0
faker>=26.0.0
ruff>=0.5.0
pre-commit>=3.7.0